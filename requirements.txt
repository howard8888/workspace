# CCA8 optional tooling and development dependencies.
# Python 3.13 standard-library modules are not listed here.
# Note that CCA8 code will run without an LLM/LRM but it may be used in particular setups, although for many
#  experimental setups we explicitly do not make use of the LLM/LRM.

openai
pyvis
pygount
psutil
pytest
# Coverage is intentionally optional and is not installed by the normal CCA8 requirements.
# See README.md for the pure-Python Coverage.py installation used with --preflight --coverage.
pylint
mypy